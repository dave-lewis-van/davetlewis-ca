---
import Logo from './Logo.astro';

const links = [
  { href: '/portfolio', label: 'Portfolio' },
  { href: '/about', label: 'About' },
  { href: '/resume', label: 'Resume' },
  { href: '/contact', label: 'Contact' },
];

const current = Astro.url.pathname;
---

<header class="border-b border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-950">
  <nav class="max-w-5xl mx-auto px-6 h-16 flex items-center justify-between">
    <Logo />
    <ul class="flex items-center gap-6">
      {links.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class:list={[
              "text-sm font-medium transition-colors px-1 -my-2 border-b-2 pb-0.5",
              current.startsWith(href)
                ? "text-blue-700 dark:text-blue-400 border-blue-700 dark:border-blue-400"
                : "text-slate-500 hover:text-blue-700 dark:text-slate-400 dark:hover:text-blue-400 border-transparent",
            ]}
          >
            {label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</header>
